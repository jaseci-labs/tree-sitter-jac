node_modules/
build/
*.log
.DS_Store

# tree-sitter generated native artifacts (grammar.json / parser.c ARE committed)
*.o
*.so
*.dylib
*.wasm
log.html

# corpus check scratch
/tmp-corpus/

# Jac compiler cache
.jac/
__jac_gen__/

# Auto-generated multi-language bindings/publishing scaffolding (not needed;
# nvim-treesitter/editors only need src/parser.c + queries/). Regenerated by
# `tree-sitter generate` but intentionally untracked.
/grammar.js
/bindings/
/Cargo.toml
/Package.swift
/Package.resolved
/setup.py
/pyproject.toml
/binding.gyp
/Makefile
/go.mod
/package-lock.json
/package.json
# NOTE: tree-sitter.json is NOT ignored -- it is the hand-authored grammar
# manifest used by editor ecosystems (Helix/Zed/tree-sitter CLI).
